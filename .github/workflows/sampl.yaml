name: SampleGitHubAction

on:
 push:
    branches:
        - main
 pull_request:
    branches:
        - main


jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Hello World
      uses: actions/checkout@v2
      run: echo "Hello Sarit"
      shell: zsh

    - name: Goodbye 
      run: echo "Goodbye Sarit"
      shell: zsh
